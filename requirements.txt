# Core dependencies
numpy<2
scipy
sounddevice

# Machine Learning (Training & Visualization)
tensorflow
tensorflow-hub
scikit-learn
matplotlib
seaborn

# Inference (Raspberry Pi / Edge)
# Note: On full PC with TensorFlow installed, you can use tf.lite.Interpreter, 
# but the scripts currently import tflite_runtime.
tflite-runtime
